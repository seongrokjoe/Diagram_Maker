# 실제 LLM 의미 분석·렌더링 개선

승인: 2026-09-10. 기준 커밋 17106dd와 internal.4 ZIP을 복구 기준으로 보존한다.

## 확정 조건
- 코드 블럭 100,000자, 전체 1,000,000자, 최대 20블럭. 업로드 없이 붙여넣기만 제공한다.
- vLLM 입력 200,000/출력 60,000 토큰. 총 문맥 미확인 시 합계 200,000을 상한으로 사용한다.
- Thinking OFF로 실제 의미 생성 검증. 검증된 요약부터 공개한다.
- 실행 예산 900초. 완료 단위 저장 후 미완료 단위를 이어간다.
- 사내에서 실제 LLM 진단을 수행하고 민감정보 없는 결과로 검증을 반복한다.

## 구현 단계
1. SVG CSS 규칙별 정제, 공통 렌더/다운로드 회귀, 동적 입력 제한 및 무손실 초과 입력.
2. 요청/단계/실패/토큰 진단, 올바른 비활성화 안내, 실제 파이프라인 합성 시험.
3. 토큰 예산·구조화 출력 협상·실패 응답을 포함한 수정, 짧은 근거 별칭, 문맥 요청 및 안전한 분할/병합.
4. 문맥 관련성 수집, 요약 우선 생성, 체크포인트·진행 표시·취소·900초 예산·이어하기.
5. 전체 verify/실제 Windows 패키지/사내 실제 LLM 검증. 실제 LLM 미검증 상태를 완료로 표시하지 않는다.

## 검증 기준
- 샘플과 5종 결과/편집/SVG/PNG에 검은 배경 회귀가 없고 외부 URL/스크립트/CSS 삽입을 차단한다.
- 입력 한도 경계와 한글/CRLF/이모지, 원문 해시·위치 복원, 큰 함수/변경 전후 문맥을 검증한다.
- 허위 호출/분기 반전/잘못된 근거/핵심 동작 누락을 거부하며 수정 피드백·반복 제한을 검증한다.
- 완료 단위는 중단/재시작/이어하기 후 재호출하지 않으며 오래된 작업자의 쓰기를 거부한다.
- 실제 LLM에서 고정 합성 사례 3회와 실패한 코드/작은 Git/큰 Git 사례의 의미·근거·시간을 확인한다.

세부 설계는 승인된 대화의 구현 계획을 따른다. 진행 및 실패는 CODE_BLOCK_DIAGRAM_PROGRESS.md에 기록한다.

## 2026-09-10 실행 결과

- 1~4단계 구현 및 로컬 회귀 검증 완료.
- 5단계 전체 verify와 `0.1.0-semantic.1` Windows 시험 패키지 검증 완료.
- 사용자가 이 PC에서 사내 LLM 검증이 불가능하다고 확정했다. 실제 모델 검증은 사내 환경에서 수행할
  미검증 항목으로 유지한다. PostgreSQL 실연결도 별도 환경 검증이 필요하다.
- 범위·결과·시험 ZIP·증적은 `SEMANTIC_RELIABILITY_REPORT.md`를 참조한다.
