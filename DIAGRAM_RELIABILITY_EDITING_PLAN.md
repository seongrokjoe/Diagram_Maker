# 다이어그램 생성 안정성·직접 편집·사용성 개선

승인: 2026-09-16. 기준 HEAD e224ae4 / internal.9. 목표 배포 internal.10.
이 파일은 대화에서 승인된 11개 요구사항과 결정 사항의 실행 기록 기준이다.

## 확정 동작

1. 자연어/코드/Git/저장소/LLM 점검 오류를 기능별로 격리한다. 비동기 오류는 시작한 기능에 귀속한다.
2. 자연어는 선택한 형식 안에서 시나리오별 여러 그림을 자동 생성한다. 원문 구간 ID로 근거를 연결하고
   전체 요구사항 배정과 그림별 구조/인터락을 검토한다. 가정은 명시한다.
3. 자연어 실행 API/저장을 추가하여 진행 복원·취소·이어하기·선택 출력 재생성을 제공한다.
   모든 그림이 실패해도 진단을 보존한다. 기존 API/단일 결과/편집 이력은 호환한다.
4. 코드/Git 설명은 함수·리비전·제어 영역·사실별로 검토한다. 실패 필드·근거·수정 지시를 구조화하고
   실패 항목만 보정 1회 후 재검토한다. 검증된 설명은 보존하고 실패 부분만 원식으로 표시한다.
5. 코드/Git 그룹 내 결과 종류는 AI와 Code 두 항목이다. AI 일부 완료/실패 상태는 AI 항목 안에 표시한다.
6. 캔버스 직접 편집: 노드·관계·클래스 멤버·시퀀스 조건/메모. 노드/관계 삭제, undo/redo, 저장/복원.
   컴파일된 요소와 IR 대응 정보를 사용하고 기존 하단 구조 편집을 유지한다. 수동 편집 출처를 보존한다.
7. Git 변경점 재선택은 마지막 그룹의 ID/제목/프리셋/형식/순서를 복원한다. 자동으로 빈 그룹을 숨겨도
   복원 정보를 계획별로 보존한다. 명시적 이동/병합은 복원 대상을 갱신한다.
8. 그룹 드롭다운에 새 그룹 만들기/즉시 배정을 추가한다. 출력 삭제는 명확한 크기의 버튼으로 표시한다.
9. 동시 LLM 요청 1개와 모든 의미 검토를 유지한다. 문맥 중복·토큰 계산·재검토를 줄이고 단계별 측정한다.
10. Git 단순 정상 변경의 AI 생성과 일부 실패의 부분 AI 보존을 실제 API/UI 경로로 검증한다.
11. 보기 모드 좌클릭 드래그 이동. 편집은 빈 공간 또는 Space+드래그 이동. 휠/확대와 공존한다.

## 순서와 완료 조건

- 단계 A: 재현/기준, 기능 오류, Git 그룹 복원·바로 만들기·삭제 버튼.
- 단계 B: 공통 근거·검토·복구와 부분 AI 보존, 결과 메뉴/집계.
- 단계 C: 자연어 원문 근거·다중 출력·실행/저장/재개·그림별 편집.
- 단계 D: 직접 편집 전체 텍스트·삭제와 캔버스 이동, 실제 SVG 상호작용 검사.
- 단계 E: 전체 verify, 332항목/1,212행·40함수 비교, 1440/390px 및 패키지 검사.
- internal.10 ZIP/SHA-256/보고서 전달. Git push는 별도 요청에 따른다. 기존 배포 ZIP은 보존한다.
- 실제 사내 LLM 품질·동일 모델 3회 중앙값 검토 시간 30% 단축은 사내 측정 전 달성으로 기록하지 않는다.
- InMemory/LocalFile/Postgres 호환·ACL·lease·마스킹·SVG strict·구형 데이터 회귀를 유지한다.
- CODE_BLOCK_DIAGRAM_PROGRESS.md에 단계별 결과/실패/다음 작업을 갱신한다.

## 완료 기록

2026-09-17 단계 A–E의 구현·로컬 검증과 internal.10 ZIP/SHA-256 전달 완료.
전체 verify 및 패키지 9단계 exit 0. 세부 결과와 외부 환경 미측정 항목은
`DIAGRAM_RELIABILITY_EDITING_REPORT.md`에 기록했다. 기존 ZIP 보존, 새 커밋·push 없음.
