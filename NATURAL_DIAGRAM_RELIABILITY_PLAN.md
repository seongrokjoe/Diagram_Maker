# 자연어 다이어그램 안정성·품질 개선 (internal.11)

승인: 2026-09-17, 기준 c0a545e/internal.10. 사용자: Implement the plan.
범위: 구현, 로컬 합성 LLM/HTTP/API/UI/저장 회귀, 오프라인 Windows ZIP/SHA와 보고서.
사내 LLM 검사는 수행하지 않는다. internal.10과 기존 체크포인트를 보존한다. Git push는 포함하지 않는다.

## 승인된 동작

1. 서버가 문장·목록·표 단위 원문 ID/UTF-16 범위를 발급한다. 모델은 ID를 선택하며 인용문을 재작성하지 않는다.
   마스킹 전후 위치를 연결하고 복수 근거/구형 인용을 지원한다. 유일한 원문 연결만 허용한다.
2. 실패 항목·필드·오류 코드·수정 지시를 구조화한다. 정상 항목은 보존하며 최초 생성 + 보정 최대 2회,
   한도/잘림은 문맥을 보존해 분할한다. 동시 요청 1개, 900초 실행 예산, 무단 입력 절단 금지.
3. 원문 준비 → 요구사항 추출/검토 → 공유 시나리오 계획 → 형식별 설계/보정 → 전체 검토를 체크포인트로 저장한다.
   입력/답변/모델/프로토콜 버전과 연결하고 이어하기에서 실패 이력·보정 횟수를 초기화하지 않는다.
4. 실제 흐름별 시나리오, 공통 엔터티/제약, 형식별 작은 계약을 사용한다. 페이지별 책임과 전체 누락/모순을 검토한다.
   Flow 분기/복귀, Sequence 순서/분기, State 경계/전이, Class 멤버/관계/인터락을 검증한다.
   긴 출력은 선택한 형식 안에서 개요/상세로 제공하고 근거/페이지 연결을 유지한다.
5. 입력 모호함은 NeedsClarification, 질문 최대 5개/한 묶음으로 저장한다. 선택지/자유 답변, 원문/이유를 제공한다.
   같은 답변 버전의 동일 질문은 반복하지 않는다. JSON/서버 문제는 사용자 질문으로 전가하지 않는다.
6. /natural-diagram-runs/{id}/answers 및 소유자 진단 다운로드를 추가한다. revision/질문 버전/ACL을 검증한다.
   기존 enum 숫자/결과/편집/API를 보존하고 natural-v7/natural-design-v3로 버전을 갱신한다.
7. InMemory/LocalFile/Postgres의 lease/취소/질문 대기/재개를 맞춘다. 파일 저장 실패는 성공으로 보고하지 않는다.
   새로고침/폴링 장애 복구, 단계/진행/질문/실패부분 재생성 UI, 안전한 진단, 정상 결과/편집 보존.
8. 20개 이상 비식별 사례를 실제 검증 경로로 시험한다. 변형 인용/공백/Unicode/마스킹/JSON/null/중복/미지 ID,
   잘림/요구사항 누락/조건 반전/공통 인터락/형식 간 모순/재시작/취소/ACL/구형 호환/1440·390px를 확인한다.
9. 전체 verify를 비동기화 작업 사본에서 실행하고 소스 해시를 대조한다. 실제 ZIP의 API/UI/작업자/실행기를 검증한다.
   ZIP 변경 안내/설치·복구 안내, SHA-256, 사례별 결과/호출수/시간/미검증 항목을 보고한다.

## 단계

A 재현/근거/진단/복구 → B 저장/질문/API → C 설계 품질/UI → D 전체 검증 → E internal.11 ZIP.
진행/중요 실패/다음 작업은 CODE_BLOCK_DIAGRAM_PROGRESS.md에 기록한다.
사내 모델 및 미제공 PostgreSQL 실연결은 합성 검증으로 대체했다고 주장하지 않는다.
