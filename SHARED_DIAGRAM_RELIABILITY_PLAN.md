# internal.14 공통 다이어그램 안정화

승인: 2026-09-22. 기준 HEAD `65c381f`, 사전 checkpoint `e53efc2` 보존.

- 자연어 설계 제안을 유지하고 요구사항별 의미 계획을 서버가 조립한다. 원문 근거와 설계 가정을 구분하며 누락을 ID 부착만으로 해결하지 않는다.
- 코드 블록/Git의 구문 분석 구조는 유지한다. 의미 설명 실패를 단위별로 격리하고 정상 결과와 정적 구조를 보존한다.
- 생성/검토 형식 복구, 의미 보정, 전송 재시도를 구분한다. 후보별 검토, 실패 범위만 재생성, 무진행 감지, 재개 시 이력 보존을 구현한다.
- 사용자 추가 요구: 횟수 소진으로 종료하는 자동 보정은 **최초 요청 + 보정 최대 10회**. 전체 시간/토큰 한도와 오류별 중단 정책은 유지한다.
- 세 입력 방식의 복구/진단/검사 계약을 정비하고 버전별 체크포인트를 분리한다. 구형 결과는 보존한다.
- 실제 사내 원문과 응답은 미제공. 코드 블록 실패의 상세 원인은 미확인이다. 합성 검증을 실제 모델 해결로 간주하지 않는다.
- 순서: 회귀 및 공통 복구 → 자연어 의미 계획/조립 → 저장/화면/사내 검사 → 전체 verify → internal.14 패키지 검증.
- 단계별 진행/중단/검증 및 다음 작업은 `CODE_BLOCK_DIAGRAM_PROGRESS.md`에 기록한다.
