# 공통 다이어그램 안정화 검증 (internal.14)

2026-09-22, 기준 `65c381f`. 승인 범위: `SHARED_DIAGRAM_RELIABILITY_PLAN.md`.
기존 체크포인트 `e53efc2`와 internal.13 패키지를 보존한다.

## 변경 동작

- 공통 보정 정책은 최초 요청 + 최대 10회이다. 응답 형식 보정, 의미 후보 보정, 전송 재시도를 구분하고
  기존 전체 시간·입력·출력 예산을 유지한다. 같은 거부 후보가 반복되면 조기에 중단한다.
- 응답별 체크포인트로 형식 복구 중 중단되어도 완료된 요청과 남은 보정 횟수를 유지한다.
  코드/Git의 내용 실패가 연속되어도 이후 독립 묶음을 생성하며, 서버 장애는 기존 중단 정책을 따른다.
- 자연어 설계는 요구사항별 의미 요소·관계를 독립 검토한 뒤 서버가 안정적인 ID와 요구사항 근거를 부여한다.
  원문에 명시된 공통 클래스·참여자는 합치되 같은 작업 이름만으로 실행 순서를 만들지 않는다.
  통합은 기존 요소 간 연결만 추가하고 전체 요구사항의 실제 동작을 다시 검토한다.
- 요구사항 ID가 붙었다는 이유만으로 의미 충족을 인정하지 않는다. 조건·인터락·근거 없는 동작은
  개별 및 통합 의미 검토 대상이다. 설계 제안은 계속 허용하며 개별 클래스 멤버에도 설계 가정을 표시한다.
- 자연어 실패 단계와 통합 검토를 UI에 표시한다. 선택 재생성 실패 시 이전 정상 결과를 유지한다.
  복구용 원시 응답은 소유자 권한이 적용된 체크포인트에만 보관하고 진단·목록·로그에 노출하지 않는다.
- 버전: `natural-v10`, `natural-design-v6`, `natural-meaning-v1`, `shared-semantic-v7`,
  `shared-requests-v9`, `diagram-recovery-v1`. 구형 저장 결과를 보존하며 DB 스키마 변경은 없다.

## 검증 진행

- `%TEMP%/DiagramMaker-natural-internal14` 비동기화 복사본에서 반입된 오프라인 캐시로 검증한다.
- 최초 재개: .NET 397 통과/2 실패. 기존 연속 3회 중단 정책의 요청 수 가정을 갱신했다.
- 추가 회귀 이후 .NET 410개 통과. 최초+10회 경계, 형식 보정 도중 재개, 후보별 검토,
  3개 실패 묶음 뒤 정상 묶음 처리, 안정적인 ID/근거, 독립 Sequence와 멤버 가정을 포함한다.
- 자연어 합성 HTTP/API/Edge 검사 `natural-reliability-LJIhxD` 통과. 질문 재시작, ACL, 상세 선택 재생성,
  390/1440px 진단·검사 요약·다운로드를 확인했다. 실제 사내 LLM과 연결한 결과는 아니다.
- 재개 중 공유 합성 서버의 구형 요청 포장 처리를 수정했고, 화면 밖으로 끝나는 드래그 검사 좌표를 보완했다.
  공유 의미 기본/60,000자 검사, SVG 6개와 Sequence 4개 검사가 통과했다.
- 최종 소스 자연어 회귀 `natural-reliability-mA9qFF`, 패키지 회귀 `natural-reliability-cAyjh6` 통과.
  390/1440px의 실패 단계·전체 진단·검사 요약·다운로드와 패키지의 internal.14 표시를 확인했다.
- 전체 `verify.ps1` 최종 exit 0: .NET 410, 작업자 43, 웹 62, 정책 13, 시작 정책 11,
  API 두 모드, 공유 기본/60,000자, SVG 6, Sequence 4, 코드·디자인·자연어 UI와
  Windows UI 실행기 14개(`ui-test-launchers-SMIKm5`)를 포함한다.
- 패키지 검사 7개 통과 후 60,000자 검사에서 리비전 저장 버튼 click 10초 시간 초과가 있었다.
  전체 소스 실행기 빌드와 동시에 수행된 검사였으며 제품 파일이나 검사 시간 제한 변경 없이
  단독 재실행에서 통과했다. 첫 결과와 실패 로그·화면은 보존했다.
- 마지막 Windows CMD 검사의 구형 `natural-design-v5` 기대값을 실제 v6에 맞춰 갱신했다.
  CMD 8개 검사(`windows-launchers-acbynb`)를 포함해 패키지 검사 9개 명령 모두 최종 exit 0이다.
  API/미리보기, 코드 UI, 디자인 UI, 자연어 API/UI, 합성 LLM 일반/기본 모드,
  공유 의미 기본/60,000자, Windows 실행기를 검사했다.
- 자연어 소스/패키지 회귀는 각각 20개 검사·406회 합성 요청으로 통과했다.
  소스 32.657초, 패키지 37.533초이며 실제 모델 성능 측정값이 아니다.
- 최종 소스 파일 312개를 다시 대조했고 `git diff --check`가 통과했다. 미해결 로컬 검증 실패는 없다.
  검증 중 확인된 실패와 보조 스크립트 수정 이력은 진행 기록에 남겼다.

## 패키지 무결성

- `DiagramMaker-0.1.0-internal.14-win-x64.zip`: 96,528,255바이트, 1,832파일.
- SHA-256: `78bc146f54f121269195aeb7a891dcb1593e52b07714fb6febf540bf9a57c2db`.
- 원본/검증 복사본의 소스·빌드·검사 파일 312개와 ZIP/stage 전체 파일의 SHA-256이 일치한다.
- manifest는 기준 커밋 `65c381f` 및 미커밋 변경 상태를 기록한다. internal.13 ZIP은 보존한다.
- ZIP/SHA는 `artifacts/release/`, 전체 검증·패키지 검사 로그와 첫 실패/최종 결과 JSON,
  소스 해시와 화면 증적은 `artifacts/internal14-validation/`에 보존했다.
- 구현·로컬 검증·패키징은 완료했다. 이번 재개에서는 커밋·원격 푸시를 수행하지 않았다.

## 미검증 범위

실제 사내 원문·모델 응답과 PostgreSQL 연결 환경은 제공되지 않았다. 실제 모델의 의미 품질·응답 시간,
PostgreSQL 실연결·다중 인스턴스 동시성, 호스트 egress 캡처와 외부 취약점 피드는 검증하지 않았다.
사내에서는 `test-natural-diagram.cmd` 및 기존 실패 입력의 신규 실행으로 실제 조건·동작 보존을 확인한다.
합성 모델의 통과를 기존 사내 오류 해결의 증명으로 간주하지 않는다.
