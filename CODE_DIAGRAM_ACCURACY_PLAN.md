# 코드 다이어그램 정확도 개선 계획

승인: 2026-09-11. 기존 코드 블럭 구현 계획을 보완한다. perf.4 소스와 패키지,
기존 문서·사용자 편집·기능 전 체크포인트를 보존한다. 목표 배포판은 `0.1.0-accuracy.1`.

1. C++/C# 공통 실행 사실: AST/CFG에서 호출, 조건 평가, 대입, 반환, 예외, 반복,
   종료 대상을 원본 위치와 함께 추출한다. 누적 조건 문자열로 실행 구조를 복원하지 않는다.
   관측 호출과 대상 심볼 해석을 구분하고 미해결 호출도 보존한다. 지역 상수·복사·분기
   병합으로 반환값을 보수적으로 추적한다. 별칭·외부 변경은 미확인으로 남긴다.
   단락 평가, 중첩 호출, 반복 평가, break/continue, C++ 인자 평가 순서 불확실성을 보존한다.
2. 공통 LLM 의미 계획: 함수 원문과 실행 사실을 함께 제공해 역할, 처리 단위, 조건,
   결과와 형식별 묶음을 구조화한다. 호출 누락·중복·재정렬, 조건 반전, 반환 변경을
   실행 사실 기준으로 거부한다. 항목별 검토와 별도로 함수 전체 동작을 검토한다.
   구현 근거와 이름 기반 해석을 구별한다. 함수/완결된 제어 영역 단위로 분할하며
   문맥과 진입·종료를 보존한다. 수정 요청은 최대 1회, 기존 예산과 thinking OFF 유지.
3. 코드 블럭과 Git의 다섯 형식: Sequence는 진입 함수 전체 호출과 실패 반환을 한
   다이어그램에 표시한다. 내부 함수는 상세 탐색, 독립 진입점은 별도 시나리오로 제공한다.
   Flow는 CFG와 실제 호출 연결을 보존하고 연속 처리가 증명될 때만 묶는다. Class는
   실제 선언만 사용한다. State는 동일 변수의 조건·대입 근거가 있는 전이만 표현한다.
   관계도는 코드/추론/미확인/사용자 관계를 구분하고 시간 순서를 발명하지 않는다.
4. Git State 추가: 두 리비전의 전이를 변수별로 비교한다. 동일 변수·출발·도착에
   조건만 변경되면 수정으로 표시하고 양쪽 근거를 제공한다. 불확실한 대응은 별개로
   남긴다. 프런트엔드의 State→Flow 변환을 제거한다.
5. 계약·표시·호환: 선택적 실행/반환/종료/확신 필드를 추가한다. 한국어 설명과 정확한
   원식을 분리하고 호출·조건·반환별 근거 탐색을 제공한다. 분석/계획/구조 검증/의미
   검토 상태를 정직하게 표시한다. 캐시 버전을 올리고 구버전 중단 실행은 불변 입력에서
   새 실행으로 시작한다. 기존 저장 결과와 수동 편집을 보존한다.
6. 수용 검증: SendInitDataRequest는 호출 7개, 조건 5개, 조기 false 반환 5개,
   최종 true 반환 1개다. 첫 실패별 5개와 정상 1개 경로의 호출 순서·반환값을 독립적인
   기대값으로 검사한다. 같은 이름의 실제 반복 호출은 제거하지 않는다. 원문의
   enumFunctionResunt를 임의 교정하거나 Sleep의 단위·외부 구현을 추정하지 않는다.
   누락/중복/재정렬/극성/반환/가짜 상태 LLM 응답을 거부하는 회귀를 추가한다.
7. 전달: 전체 verify, SVG/UI 근거·편집·복원·다운로드, Windows 패키지 검사를 완료하고
   소스/ZIP/SHA/증적을 기록한다. 실제 사내 LLM·PostgreSQL 실연결은 이 PC에서 하지
   않으며 합성 테스트와 구별한다. 각 단계 및 중단 시 진행 기록과 다음 작업을 갱신한다.
