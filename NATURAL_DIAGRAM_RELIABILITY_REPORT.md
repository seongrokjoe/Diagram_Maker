# 자연어 다이어그램 안정성 검증 기록 — internal.11

기준 커밋: `c0a545ec442cf0c7b64eeacb1230df97d2939f07`.
승인 범위: [구현 계획](NATURAL_DIAGRAM_RELIABILITY_PLAN.md)의 로컬 합성 검증과 Windows ZIP.
구현·검증 단계에서는 기존 internal.10과 체크포인트를 보존했다. 이후 배포 정리에서 internal.11만 유지한다.

## 변경 동작

- 서버가 문장·목록·표의 원문 근거 ID와 UTF-16 범위를 발급한다. 복수 근거와 마스킹 전후 위치를
  유지하며 구형 인용은 유일하게 연결될 때만 허용한다.
- 요구사항 추출·누락 검토, 시나리오별 설계·검토, 형식 간 최종 검토를 수행한다.
  최초 생성 뒤 보정은 최대 2회이며 정상 항목을 보존한다. JSON 오류를 사용자 질문으로 전가하지 않는다.
- 실행 체크포인트와 진단을 저장한다. 이어하기는 완료한 요청을 재사용하고 이미 소진한 보정 횟수를 유지한다.
  입력·답변·모델·프로토콜이 바뀌면 해당 결과를 다시 검증한다.
- 출력 한도에 걸리면 선택한 형식 안에서 상세 페이지로 나누며 공통 엔터티와 인터락을 각 부분에 유지한다.
  상세 한 페이지만 재생성할 수 있고, 최종 검토가 실패해도 개별 검토를 통과한 페이지를 보존한다.
- 입력 의도가 모호하면 최대 5개 질문을 한 번 저장하고 선택지 또는 자유 답변을 받는다.
  서버 재시작·새로고침 후 복원하며 revision·질문 버전·소유자 ACL을 검증한다.
- 자연어 실행 진단 다운로드와 단계 표시를 제공한다. 질문 대기는 “답변 대기”로 표시하며,
  일시적인 폴링 실패는 자동 복구 후 안내를 지운다.
- LocalFile 쓰기 실패 시 메모리 상태를 롤백한다. 기존 enum 값·결과·편집은 유지하고
  새 생성 버전은 `natural-v7`, 설계 계약은 `natural-design-v3`으로 구분한다.

## 검증 사례

신규 `NaturalReliabilityTests`는 아래 21개 비식별 사례를 실제 추출/저장 경로에서 검증한다.

| 사례 | 검증 내용 |
|---|---|
| 1–3 | 한글·이모지·CRLF, 목록, 표의 정확한 UTF-16 원문 범위 |
| 4–8 | token, Bearer, 여러 줄 개인 키, 짧은 password, 빈 문단의 마스킹 위치 보존 |
| 9–12 | 중복 인용 거부, 공백 차이 허용, 조건 반전 인용 거부, Unicode 인용 |
| 13 | 복수 근거 보존 및 미지·중복 근거 ID 거부 |
| 14 | 문단을 넘는 시나리오와 공통 인터락 유지 |
| 15 | 실패 항목 보정 시 기존 정상 항목 보존 |
| 16 | 추출·검토 요청의 비밀값 제거와 저장 근거 보존 |
| 17 | null JSON 3회 한도 및 이어하기의 추가 전송 0회 |
| 18 | 검토 중단 뒤 저장된 추출 결과 재사용 |
| 19 | 잘린 추출을 분할하고 모든 근거 유지 |
| 20 | 질문 영구 저장, lease 해제, 답변 전 작업자 재실행 차단 |
| 21 | 파일 쓰기 실패 후 미저장 성공 상태 노출 방지 |

추가 `smoke-natural-reliability.mjs`는 실제 loopback HTTP와 LocalFile·Edge로 아래를 검증한다.

| 사례 | 결과 및 합성 HTTP 요청 수 |
|---|---|
| 정상 생성·근거·마스킹·ACL·진단 | 정상 생성 5회, 다른 소유자의 실행/답변/진단 접근 403 |
| null JSON | 3회 후 실패, 이어하기 추가 전송 0회 |
| 미지 근거 ID | 3회 후 실패, 이어하기 추가 전송 0회 |
| 조건 반전 검토 거부 | 생성/검토 6회 후 기존 정상 결과 유지 |
| Flow/Sequence/State/Class | 공유 추출·검토 2회 + 형식별 8회 + 최종 검토 1회 |
| 형식 간 최종 거부 | 5회, Partial과 개별 정상 페이지 유지 |
| 출력 잘림·선택 상세 재생성 | 분할 생성 8회, 상세 하나 재생성 3회, 나머지 원본 유지 |
| 질문·재시작·오래된 답변·통신 장애 | 질문까지 2회, 답변 뒤 5회, stale 요청 409, 1440/390px UI |

재개 후 최종 소스 통합 `natural-reliability-dImPXW`는 총 51회 합성 요청, 12.428초였고,
패키지 통합 `natural-reliability-kMCFGx`는 동일한 51회, 21.336초였다.
사례별 요청 시각·종류·모드와 전체 경과 시간은 각 `result.json`에 보존한다.
이 시간은 의도적으로 즉시 응답하는 합성 서버의 수치이며 실제 모델 처리 시간이나 의미 품질 지표가 아니다.

## 전체 검증 및 배포

최종 전체 검증은 2026-09-21 13:54:18–14:04:32 KST에 실행해 **exit 0**으로 완료했다.
비동기화 사본에서 `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\verify.ps1 -UseInstalledDependencies`를 실행했다.
패키지 공유 의미 두 입력 한도와 CMD 검증도 14:09:59 KST에 모두 **exit 0**으로 완료했다.
.NET 370, 작업자 43, 웹 62, 정책 13, 시작 정책 11개 통과.
API 두 모드, 공유 의미 두 입력 한도, SVG 6개, Sequence 4개, 코드·디자인·자연어 UI와 CMD 14개가 통과했다.
코드 UI의 모바일→데스크톱 전환 뒤 ResizeObserver 갱신 대기를 보완하고 단독 재검증을 통과했다.
초기 esbuild 샌드박스 읽기 제한은 비동기화 사본의 확장 권한 검증으로 처리했다.
재개 전 검증은 CMD/공유 의미 검사 도중 중단되어 완료로 간주하지 않았다.
동일한 검증 사본에서 전체 검증을 재실행해 실제 종료 코드와 시각을 확인했다.
CMD의 변경 감지·실패한 빌드의 기존 서버/데이터 보존·손상된 캐시 복구·프로세스 식별·정책 검사는
`ui-test-launchers-gRgDgB`에서 통과했다.

| 최종 검증 | 결과/증적 |
|---|---|
| 전체 소스 검증 | exit 0 |
| 패키지 빌드·API·코드 UI·디자인 UI·자연어 UI·합성 LLM 두 모드 | 각 exit 0, 기존 완료 결과와 동일 소스/ZIP 확인 |
| 패키지 공유 의미 기본 한도 | exit 0, `shared-semantics-8bbLQe` |
| 패키지 공유 의미 60,000자 한도 | exit 0, `shared-semantics-sxTg2n` |
| 배포 CMD | exit 0, 7개 검사, `windows-launchers-Mm2Cog` |

패키지 공유 의미 두 조건 모두 요청 8/10/20회·결과 41/43/51페이지를 유지했고,
Git 5종·부분 AI·독립 원본 및 자연어 직접 편집·선택 재생성·복원을 통과했다.
전체 소스 검증과 패키지 빌드/검사 10단계의 종료 결과를 확인했다.

배포 파일: [internal.11 ZIP](artifacts/release/DiagramMaker-0.1.0-internal.11-win-x64.zip),
[SHA-256](artifacts/release/DiagramMaker-0.1.0-internal.11-win-x64.zip.sha256).
96,472,718바이트, 1,831파일을 검증 완료한 stage와 모두 SHA-256으로 대조했다.
ZIP SHA-256: `ba9e58c272e64d32a48b279b052083dda17e094f336ce462d99ae0ab227ed585`.
manifest는 기준 커밋과 `sourceTreeDirty=true`를 기록한다. 배포 정리 후 release 폴더에는 internal.11만 유지한다.

소스 334개를 검증 사본과 해시로 대조했다. 이후 변경은 README·진행 기록·배포 포인터이며
실행 소스는 동일하다. 검증 중 수집한 중간 화면·로그는 배포 정리 과정에서 제거했고 최종 결과는 이 보고서에 남겼다.
`git diff --check` 통과, 미해결 로컬 검증 실패 없음.

패키지의 1440px 질문 화면과 390px 완료 화면을 직접 확인했다.
질문 대기의 단계 표시, 답변 입력, 통신 복구 후 오류 안내 제거와 최종 검토 단계를 확인했다.

## 설치와 한계

새 ZIP은 별도 비동기화 로컬 폴더에 풀고 기존 서버를 종료한 뒤 data와 승인된 설정을 백업·이전한다.
동일한 LocalFile 저장소를 두 프로세스가 동시에 열지 않는다. 되돌릴 때는 이전 패키지와 업데이트 전
백업을 함께 사용한다. 동봉 `OFFLINE_INSTALL_KO.txt`와 `INTERNAL_TEST_KO.txt`에 절차를 반영했다.

사내 실제 LLM의 의미 품질·응답 시간, PostgreSQL 실연결·동시성, 호스트 egress 캡처 및 외부 취약점
피드 검사는 미수행이다. 합성 검증과 오프라인 라이선스 검사를 해당 검증으로 간주하지 않는다.
