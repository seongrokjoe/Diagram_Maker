# 코드 블럭·Git 다이어그램 생성 성능 개선

승인: 2026-09-10. 기준 `f6be5d3`; internal.5 ZIP 및 기존 변경/체크포인트를 보존한다.
사용자는 코드 블럭 계획 실행과 함께 Git 변경 분석에서도 같은 문제가 있는지 확인하고 동일 계열 개선을 적용하도록 지시했다.

## 사용자 확정 조건

- 약 1,200줄의 여러 함수·클래스가 있는 C++ 코드, 여러 형식 선택에서 전체 상세 완성 후 표시한다.
- Thinking OFF의 실제 사내 환경에서 전체 완료 5분 이내를 성능 목표로 한다. 5분 초과 시 기존 900초까지 계속한다.
- 모든 완료 작업을 저장하며 두 차례 이상의 이어하기와 재시작에서도 재호출·집계 초기화를 방지한다.
- 실제 사내 LLM은 이 PC에서 검증할 수 없다. 합성 계약/성능 검증과 실제 모델의 5분 달성을 구분한다.

## 확인한 원인

- 코드 블럭: 함수·타입별 이해 + 그룹 추천 + 페이지별 생성/검토. 합성 1,212줄/4클래스/40함수에서 Flow만 기본 127회(LLM 호출 없이 파서와 호출 구조로 산출).
- Git: 그룹 이해 후 각 형식의 페이지마다 생성/검토. 같은 사실·변경 전후 본문이 반복 전송된다.
- 공통: 요청은 진단 개수로 집계되어 전송 전 실패를 포함하고 HTTP 재시도/토큰 계산과 다르다.
- 공통: 체크포인트는 이어받지만 완료/재사용 카운터는 0에서 시작한다. 실패/거절/분할 진행 저장이 부족하다.
- Git: 처리 시작 시 기존 부분 결과를 정적 초기 결과로 교체하는 경로가 있다.

## 구현 단계

1. 공통 실행 기록: 논리 작업과 실제 전송을 분리 계측, 누적/이번 실행 집계, 안정적 체크포인트/실패/분할 상태, 취소 및 저장 보호.
2. 코드 블럭: 선택 형식의 사실 합집합을 한 번 의미 분석하고 검토하는 묶음 처리. 함수/페이지별 중복 LLM 해석을 없애고 공유 의미 모델로 전체 페이지를 구성한다.
3. Git: 동일 묶음/공유 모델을 적용하되 변경 전후 SHA, 변경 범위, 유지/추가/삭제 설명 및 refinementInstruction을 보존한다. 이전 결과를 재개 시 제거하지 않는다.
4. 입력 48,000/생성 출력 8,000/검토 출력 2,000토큰을 묶음 기본값으로 사용하되 기존 서버 상한을 존중한다. 실제 출력 예약과 토큰 계산을 사용하며 완전한 구문·제어 경계로 분할하고 원문을 자르지 않는다.
5. 정상 묶음당 생성/검토 각 1회, 의미 수정 최대 1회. 분할과 실패도 저장하고 이어하기가 수정 한도를 초기화하지 않도록 한다. 동일 영구 검증 실패 3묶음 연속 시 조기 중단한다.
6. API 경로는 유지하고 선택 필드/진단 v2를 추가한다. UI는 전체/이번 실행 진척·요청·대기 시간을 구분하고 완료 후 결과 공개, 중단 시 부분 결과 열기를 제공한다.
7. 기존 작업/결과 읽기 및 구버전 이어하기를 보존한다. 새로운 실행에만 새 생성 버전을 적용한다. 기존 소유자/lease/CAS/원문 비로그/정적 검증은 유지한다.

## 검증 및 완료 기준

- 고정 1,200줄/40함수 이상/다중 클래스 C++와 동일한 Git 변경 사례에서 Flow 및 다중 형식 요청 수 회귀. 정상 코드 블럭 총 생성/검토 요청 12회 이하를 목표로 고정한다.
- 사실·분기·반환·반복·오류·교차 함수 호출, 상태 근거, Git 변경 전후 의미, 원본 근거와 상세 연결 보존.
- 생성 후 검토 전/검토 중/페이지 구성 중 중단, 두 차례 이어하기와 LocalFile 재시작, 소유자/동시성/취소/900초/집계 회귀.
- 잘린 출력, 계약/의미 거절, 문맥 초과, 토큰 계산 실패, 통신 실패와 연속 실패에 유한한 처리.
- 전체 verify는 비동기화 작업본에서 실행한다. 실제 Windows API/UI/실행기/ZIP 검증 후 새 시험 패키지 생성, internal.5 복구본 보존.
- 사내 고정 사례와 실제 문제 코드 각각 3회/Thinking OFF/전체 300초 이내를 별도 검증한다. 미실시를 성공으로 기록하지 않는다.

현재 진행/재개 명령과 실패는 `CODE_BLOCK_DIAGRAM_PROGRESS.md` 맨 위에 기록한다.
