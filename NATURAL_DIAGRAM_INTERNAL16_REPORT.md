# 자연어 다이어그램 실패 복구 결과 (internal.16)

## 문제와 원인

사용자가 전달한 진단에서는 표 공통 조건 사례가 NATURAL_PLAN_INVALID로, Wafer 이송 사례가 NATURAL_DESIGN_REJECTED와 NaturalConditionChanged·NaturalNodeInvalid·NaturalRepairNoProgress로 끝났다. 이 기록만으로 사내 GLM 5.2 응답 자체가 잘못됐다고 단정할 수는 없다. 서버의 시나리오 매핑·구조 검증·의미 검토·보정 흐름도 실패 원인이 될 수 있었다.

로컬 합성 API/UI 검사에서 서버 결함 하나를 재현했다. 원문의 비밀 값을 마스킹해 LLM에 보냈을 때, 검토기가 모델이 본 마스킹된 문구의 정확한 인용을 마스킹 전 원문과만 비교하여 NaturalReviewEvidenceInvalid로 거부했다. 이제 원문과 모델에 전달된 마스킹 표현 양쪽을 근거로 대조한다. 실제 사용자 입력의 실패 원인 전체가 이 결함이었다고 주장하지는 않는다.

## 변경 내용

- 시나리오 매핑 응답에서는 모델이 원문 범위 ID를 직접 만들지 않는다. 서버가 요구사항 근거에서 계산한다. 비어 있는 시나리오, 중복 ID, 알 수 없는 요구사항, 누락된 요구사항, 잘못된 질문 근거를 별도 코드와 대상 필드로 진단한다.
- 노드·전이·조건 경로·근거 연결 오류를 요소 ID와 필드 단위로 기록한다. 보정 요청에는 이 지적을 전달하고, 동일 후보가 반복되면 더 이상의 무의미한 반복을 중단한다.
- 의미 검토의 조건 변경 지적에는 실제 원문 인용과 관련 요소 ID를 요구한다. 근거가 유효한 지적은 동일 후보를 한 번 더 검토한다. 검토가 철회되면 후보를 채택하고, 재확인된 오류만 보정 대상으로 삼는다.
- 소유자만 조회 가능한 진단 비교 API/UI에 원문 발췌, 지적 대상, 실제 생성 값, 수정 지시를 연결한다. 값은 길이를 제한하고 비밀을 마스킹한다. 일반 진단 다운로드에는 이 비교 본문을 포함하지 않는다.
- 관리자 고정 검사에 사용자가 제공한 Load Buffer·Dipping Buffer·WTR·Wafer Handshake 문장을 그대로 사용하는 Sequence 사례를 추가했다. 검사 요약에는 형식과 조건 변경 재확인 횟수를 표시한다.
- 생성 계약은 natural-design-v8, 생성기는 natural-v12이다. 기존 저장 결과와 수동 편집을 변환하지 않는다.

## 검증

비동기화 로컬 사본에서 전체 verify가 exit 0이었다. .NET 434개, Node 작업자 43개, 웹 빌드와 테스트, 정책·라이선스 검사, API/UI 검사, 공유 의미 기본·60,000자 검사, 자연어 합성 API/UI 검사 21항목, Windows UI 실행기 14항목이 통과했다. 수정한 마스킹 인용 .NET 회귀 테스트 5개와 자연어 API/UI 검사는 별도로도 통과했다.

internal.16 Windows 패키지에서 미리보기, 코드 UI, 디자인 UI, 자연어 API/UI, LLM 기본·간편, 공유 의미 기본·60,000자, Windows CMD 실행기 총 9개 검사가 통과했다. ZIP 1,832개 파일 모두 stage 파일과 개별 SHA-256이 일치했고, ZIP 자체의 SHA-256도 동봉된 파일과 일치했다. 검증 로그는 artifacts/internal16-validation/에 보관했다.

## 배포 및 남은 확인

배포 파일: artifacts/release/DiagramMaker-0.1.0-internal.16-win-x64.zip 및 같은 이름의 .sha256.
ZIP 크기: 96,569,807바이트.
SHA-256: 2c4379eb4710860fb0226cc520322af68dabfe039692c5fc1eaeae7297a3cbee.
비동기화 검증 사본에서 빌드했으므로 패키지 manifest의 sourceCommit은 unavailable이다. 최종 소스 커밋과 위 ZIP 해시를 함께 대조한다.
이전 internal.15 ZIP/SHA는 배포 폴더에서 제거했고 Git 이력에는 남는다.

이 PC에서는 사내 실제 GLM 5.2, PostgreSQL, 호스트 egress와 실제 모델 응답 시간을 확인할 수 없었다. 사내 환경에서는 Thinking을 끈 상태로 짧은 승인 흐름, 표 공통 조건, Buffer Handshake 고정 검사를 새로 실행하고, 실제 Wafer 문장으로 새 생성 결과·완료 페이지·실패 진단의 원문/생성 값 비교를 확인해야 한다. 이 현장 검증 전까지 실제 모델에서 문제가 완전히 해결됐다고 판정하지 않는다.