# 클릭으로 현재 소스 UI 테스트

1. 저장소 최상위 폴더의 **`start-ui-test.cmd`를 더블클릭**합니다.
2. 현재 작업본을 확인하고 UI·API·C++ 분석 작업자를 준비합니다. 준비가 끝나면 기본 브라우저가 열립니다.
3. 소스를 수정한 뒤 **`start-ui-test.cmd`를 다시 실행**하면 변경분을 빌드하고 테스트 서버를 교체합니다.
4. 테스트가 끝나면 **`stop-ui-test.cmd`를 더블클릭**합니다.

기본 주소는 `http://127.0.0.1:5081`입니다. 다른 프로그램이 사용 중이면 5082~5101에서 빈 포트를 선택합니다.
변경이 없으면 검증된 빌드와 실행 중인 서버를 재사용합니다. 내용 해시로 변경을 확인하므로 파일 추가·삭제와 수정 시각이 같은 변경도 반영합니다.
빌드 중에는 기존 서버가 유지되며, 빌드 성공 후 해당 UI 테스트 서버만 교체합니다. 실패하면 오류를 표시하고 기존 서버와 데이터를 보존합니다.
브라우저를 닫아도 서버는 계속 실행됩니다.

## 준비 사항

- **npm을 포함한 Node.js 24 이상**과 **`global.json`이 지정하는 .NET SDK**가 PATH에 있어야 합니다. 현재 SDK는 9.0.310이며 같은 기능 대역의 최신 패치를 허용합니다.
- npm 잠금 파일과 NuGet 잠금 파일에 해당하는 의존성이 로컬 캐시에 사전 반입되어 있어야 합니다. 실행기는 `npm ci --offline`과 패키지 소스가 비어 있는 NuGet 설정으로 복원합니다.
- 기본 캐시는 `%LOCALAPPDATA%\npm-cache`와 `%USERPROFILE%\.nuget\packages`입니다. 별도 반입 경로는 `npm_config_cache`와 `NUGET_PACKAGES`로 지정할 수 있습니다.
- SDK·Node·패키지를 자동 다운로드하지 않습니다. 기존 릴리스 ZIP은 이 실행기에서 사용하지 않으며 그대로 보존됩니다.

## 실행 위치·데이터·범위

- OneDrive 안의 프로젝트도 소스 입력으로 읽을 수 있습니다. 빌드 스냅샷·앱·로그·테스트 데이터는 동기화되지 않는 `%LOCALAPPDATA%\DiagramMaker\UiTest\<저장소별 식별자>`에 둡니다.
- `src`, `web`, `tools/git-worker`, `scripts`와 루트 빌드 설정을 복사합니다. 생성물·의존성 설치 폴더·사용자 데이터·로컬 비밀 설정은 제외합니다. 빌드 도중 입력이 바뀌면 중단하므로 다시 실행하세요.
- `data`에는 저장한 테스트 작업과 생성 이력이 남습니다. 시작·종료·재빌드로 지우지 않습니다. `logs`에는 빌드와 서버 로그가 남고, `apps`에는 검증된 빌드가 저장됩니다.
- LLM은 항상 꺼진 상태입니다. 자연어 샘플, 블럭·그룹·관계 편집, 저장 및 정적 구조 확인 등을 테스트할 수 있습니다. 실제 모델 품질 확인은 기존 사내 LLM 실행 환경에서 수행하세요.
- 새 결과 화면에서는 **정적 구조 열기** 또는 **Code 다이어그램 보기 (정적 구조)**로 Code 원본을 확인합니다. 이름 검색, 상단 보기 방향, 원본 기준 100–1600% 확대·맞춤 보기, 펼치는 노드/관계 편집을 사용할 수 있습니다. AI/Code 함께 보기는 해당 실행에 두 원본이 모두 있는 경우 표시됩니다.
- Git 메뉴는 **저장소 관리**에서 본인 소유의 동기화되지 않는 로컬 저장소를 등록하면 사용할 수 있습니다. OneDrive 저장소 분석, 원격 clone/fetch와 사용자 코드 실행은 허용하지 않습니다.
- 명시적으로 지정한 `DIAGRAMMAKER_NETWORK_POLICY_PATH`를 존중합니다. UI 테스트 경로가 해당 정책에서 허용되지 않으면 중단합니다.
- 기존 앱의 데이터·LLM 설정·5080 서버는 유지합니다. 이 실행기는 사내 배포/LLM 시험용 기존 실행기를 대체하지 않습니다.

## 문제가 생기면

- 오류 창은 자동으로 닫히지 않습니다. 표시된 설명과 테스트 폴더의 `logs`를 확인하세요.
- SDK/Node 누락: 위 준비 사항에 맞는 도구를 설치하고 PATH에서 실행 가능한지 확인하세요.
- 의존성 복원 실패: 잠금 파일에 맞는 npm/NuGet 캐시를 반입한 뒤 다시 실행하세요.
- 소스 빌드 실패: `*.build.out.log`와 `*.build.error.log`에 표시된 컴파일 오류를 수정하세요. 기존 서버가 남아 있다면 그 화면은 이전 빌드입니다.
- 작업이 진행 중이라는 안내: 먼저 실행한 시작/종료 작업이 끝난 뒤 다시 누르세요.
- 캐시 파일이 변경되거나 누락되면 새 폴더에 다시 빌드합니다. 테스트 데이터는 보존합니다.
- 화면이 준비되지 않음: 로그를 확인하고 `stop-ui-test.cmd` 실행 후 다시 시작하세요.

브라우저 없이 서버만 시작하거나 시작 포트 범위를 바꾸려면:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\ui-test.ps1 -NoBrowser -Port 5090
```

## 실행기 검증

2026-09-21 internal.11을 포함한 최신 전체 `verify.ps1`은 실제 종료 코드 0으로 완료했습니다.
.NET 370개·작업자 43개·웹 62개·정책 단위 13개, API·공유 의미 두 설정·전체 UI와 CMD 14개가 통과했습니다.
모바일 오류 표와 AI/Code 비교 화면을 포함한 최신 결과는 [자연어 안정성 검증 보고서](NATURAL_DIAGRAM_RELIABILITY_REPORT.md)를 참고하세요.

비동기화 작업본에서 다음 명령으로 재현할 수 있습니다. `scripts/verify.ps1`도 두 검사를 포함합니다.

```powershell
node --test scripts/ui-test.test.mjs
node scripts/smoke-ui-test-launchers.mjs
```

실제 CMD 검사는 합성 데이터로 UI/API 변경 반영, 파일 추가·삭제, 변경 없는 재실행, 컴파일 실패 시 기존 서버 보존,
자동 교체, 저장 데이터·생성 결과 복원, 포트 충돌, 프로세스 신원 확인, 캐시 손상 복구, 정책·SDK 오류를 확인합니다.
검증용 데이터와 로그는 실행 중 `artifacts/ui-test-launchers-*`에 생성되며 배포 정리 시 제거할 수 있습니다.
최종 결과와 미검증 항목은 최신 검증 보고서와 진행 기록에 유지합니다.
