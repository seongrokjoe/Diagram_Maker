# 코드 블럭·Git 생성 성능 검증 기록

기준: `f6be5d3`, 기존 복구 체크포인트와 Git 이력 보존. 새 시험 버전: `0.1.0-perf.1`.

## 변경한 동작

- 선택한 형식의 원본 사실을 합쳐 의미를 생성·검토하고 여러 요약/상세 페이지에 적용한다.
- 같은 파일·함수 범위는 같은 묶음에 배치하며 원문 근거의 반복 전송을 줄인다. Git 변경 전후 SHA와 두 원문, 사용자 요청은 보존한다.
- 입력 48,000/생성 출력 8,000/검토 출력 2,000토큰을 기본 묶음 한도로 사용한다. 기존 서버 상한과 Thinking 출력 예약을 존중한다.
- 완료된 생성·검토와 거절·분할 상태를 저장한다. 재개 시 완료 요청을 재전송하지 않으며 수정 횟수를 초기화하지 않는다. 연속 영구 실패는 3묶음에서 중단한다.
- 전체 상세 완성 후 결과를 표시하고 중단 시 부분 결과를 열 수 있다. 진단 v2와 화면에 전체/이번 실행의 완료·요청·실제 전송·대기 시간을 구분한다.
- 기존 저장 형식은 선택 필드 추가로 호환된다. 이전 실행은 저장된 생성 버전으로 이어가며 새 실행부터 공유 의미 방식을 사용한다.

## 합성 검증

1,212줄·4개 클래스·40개 함수 C++를 실제 파서, API, 저장소, HTTP 전송 계층으로 실행했다. LLM 응답은 계약 검사용 합성 응답이다.

| 사례 | 생성·검토 HTTP 요청 | 결과 페이지 |
| --- | ---: | ---: |
| 코드 블럭 Flow | 4 | 41 |
| 코드 블럭 Flow·Class·코드 관계도 | 6 | 43 |
| 같은 C++의 Git 변경, 3개 형식 | 20 | 51 |

동일 코드 결과 재사용은 추가 요청 0회였다. 코드 블럭 요청 수 목표는 12회 이하이며, 기존 Flow 경로의 기본 127회는 실행 구조로 산출한 비교값이다. 합성 결과를 실모델 속도·품질 측정으로 간주하지 않는다.

공유 의미 회귀 11개에서 코드 블럭/Git의 두 차례 LocalFile 재시작·재개, 페이지 저장 중 두 차례 중단, 완료 HTTP 요청 중복 방지, 누적 집계, 소유자/동시성, 잘린 출력/계약 거절/의미 거절의 유한 처리, 다섯 형식의 제어·호출·상태·사용자 근거와 상세 연결을 확인했다.

배포 실행 파일에서도 같은 4/6/20회 결과를 확인했다. 실제 API 증적은
`artifacts/performance-validation/shared-semantics-Rc2jFJ/result.json`에 보존했다.

## 전체 검증과 시험 패키지

2026-09-11 비동기화 작업본에서 `scripts/verify.ps1` 전체 통과(exit 0).

- .NET 195/195, Git 작업자 32/32, 프런트엔드 46/46, 정책 7/7.
- 시작 정책 11개, 실제 API 두 모드, 공유 의미 API 요청 수, SVG 6종, 실제 Edge UI 통과.
- UI는 생성 중 결과 페이지 요청 0회, 전체/이번 실행 집계, 5분 초과 안내, 중단 후 부분 결과 열기를 확인했다. 진행 표시는 1440px와 390px에서 확인했다.
- `0.1.0-perf.1` 빌드 후 배포 EXE·Node로 미리보기, 코드 블럭 UI, 공유 의미 API, LLM 전송 두 모드(각 5개), Windows 시작 스크립트(6개) 검사 모두 통과했다.

시험 파일: `artifacts/release/DiagramMaker-0.1.0-perf.1-win-x64.zip` 및 같은 이름의 `.sha256`.
크기 94,208,344바이트, 1,818개 파일. SHA-256:
`2c9b0049d3b442efe41a29ba27f5cbecb8c4e0a8498e0243927590faac9ad99e`.

ZIP의 모든 파일이 실제 검증한 배포 폴더와 일치하며, 코드 블럭 파서·프런트엔드·동봉 Node·갱신한 사내 시험 안내가 포함된다.
기존 internal.5 ZIP은 SHA-256 `c71bfe6b35cdd71ce047766426039f5b2cd5e5482408fac74ceba310616ef2ed`를 확인한 뒤,
2026-09-11 사용자 요청에 따라 배포 폴더에서 ZIP과 체크섬을 정리했다. 현재 배포 폴더에는 perf.1 두 파일만 있다.
삭제 목록은 `artifacts/performance-validation/removed-packages.json`이며 기존 파일은 `f6be5d3` Git 이력에서 복구할 수 있다.

검증 로그·화면·진단은 `artifacts/performance-validation/`에 있다. `package-audit.json`은 ZIP 대조 결과,
`source-inventory.json`은 저장소와 검증 작업본이 일치하는 소스 212개의 SHA-256이다.
시험 패키지는 빌드 당시 기준 `f6be5d3` 이후의 작업본 변경을 포함한다. `.git` 없는 비동기화 작업본에서 빌드했으므로
패키지 manifest의 `sourceCommit`은 `unavailable`, `sourceTreeDirty`는 null이며 정확한 소스는 위 목록으로 식별한다.
2026-09-11 사용자 요청으로 소스·시험 ZIP·검증 증적과 구버전 정리를 함께 커밋·푸시한다.
빌드 후 제품 소스 212개와 ZIP 해시가 검증 당시와 같음을 재확인했으며 미해결 로컬 검증 실패는 없다.

## 실행 명령과 사내 시험

비동기화 작업본에서 `powershell -ExecutionPolicy Bypass -File .\scripts\verify.ps1`을 실행한다. 요청 수 회귀는 `node scripts/smoke-shared-semantics.mjs`이며 실제 패키지 검사 시 `artifacts/stage/DiagramMaker-0.1.0-perf.1-win-x64`를 인수로 전달한다.

이 PC에서는 사내 실제 LLM을 검증하지 않는다. 사내 고정 코드와 실제 문제 코드를 각각 3회, Thinking OFF, 전체 300초 이내 조건으로 별도 시험해야 한다. 5분 초과 시에도 900초까지 계속 실행한다. 의미·분기·호출·근거의 품질을 함께 확인하며 안내는 패키지의 `INTERNAL_TEST_KO.txt`에 포함한다. 실제 PostgreSQL 연결 검증도 이번 합성 결과에 포함하지 않는다.
